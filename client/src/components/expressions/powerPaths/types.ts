export interface PowerPath {
    id: number;
    name: string;
    description: string;
}

export interface CreatePowerPath {
    expressionId: number;
    name: string;
    description: string;
}

export interface EditPowerPath {
    id: number;
    name: string;
    description: string;
}

